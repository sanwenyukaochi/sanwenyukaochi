name: ZeroTier-One

services:
  ZeroTierOne:
    container_name: ZeroTier-One
    image: zerotier/zerotier:1.14.2
    cap_add:
      - NET_ADMIN
      - SYS_ADMIN
    devices:
      - /dev/net/tun:/dev/net/tun
    mem_limit: 200m
    environment:
      TZ: Asia/Shanghai
    volumes: [ZeroTierOne-Data:/var/lib/zerotier-one]
    network_mode: host
    restart: unless-stopped

volumes:
  ZeroTierOne-Data:
    name: ZeroTierOne-Data
